{
  "name": "amso-frontend-box",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch index.js --watch backend/index.ts --exec \"tsc --project tsconfig.backend.json && node index\" ",
    "build": "tsc --project tsconfig.backend.json",
    "start": "cross-env NODE_ENV=production  && node index"
  },
  "author": "amso",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cross-env": "^7.0.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.2"
  }
}
